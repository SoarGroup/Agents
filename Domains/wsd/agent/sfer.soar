source agent.soar

sp {apply*init-agent*conditions
    (state <s> ^operator.name init-agent)
-->
    (<s> ^params <param>)
    (<param> ^epmem-recog no
             ^epmem-retrieve yes
             ^smem-recog yes
             ^smem-retrieve yes)
}
