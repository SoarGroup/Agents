sp {get-digit1*elaborate*state
   (state <s> ^name get-digit1
              ^superstate.current-column <c>)
   (<c> ^digit1 <a>
        ^carry <cb>) 
-->
   (<s> ^digit1 <a>
        ^digit2 <cb>)
}


